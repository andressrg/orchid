ALTER TABLE "orchid_session" ADD COLUMN IF NOT EXISTS "transcript_search" "tsvector" GENERATED ALWAYS AS (to_tsvector('english', left(coalesce("transcript", ''), 250000))) STORED;--> statement-breakpoint
CREATE INDEX "idx_orchid_session_transcript_fts" ON "orchid_session" USING gin ("transcript_search");